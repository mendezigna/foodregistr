export interface Dictionary {
  auth: {
    login: string;
    noAccount: string;
    haveAccount: string;
    register: string;
    placeholder: {
      email: string;
      password: string;
      repeatPassword: string;
      name: string;
    };
  };
}
